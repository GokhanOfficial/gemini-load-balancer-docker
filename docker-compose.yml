version: '3.8'

services:
  gemini-load-balancer:
    build: .
    ports:
      - "7171:7171"
    environment:
      - GEMINI_API_KEYS=${GEMINI_API_KEYS}
    env_file:
      - .env
    restart: unless-stopped